---
description:
globs:
alwaysApply: true
---

# AI behavioral rules
- use chrome-devtools tool to run tests, inspect and debug the web app whenever required.
- always verify the code implementation plan by searching with `perplexity-ask` tool when it involves external service or library.
- For any request requiring up-to-date documentation or web searches, first verify the latest information using the configured `perplexity-ask` tool.
- Provide links to any referenced external documentation.